import Buttons from './Buttons';
import PlayButton from './PlayButton';
import EndedButton from './EndedButton';
import ReloadButton from './ReloadButton';

export {
    PlayButton,
    EndedButton,
    ReloadButton,
};
export default Buttons;
