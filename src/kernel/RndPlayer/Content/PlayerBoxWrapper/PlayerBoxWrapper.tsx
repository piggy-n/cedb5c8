import s from './styles/playerBoxWrapper.scss';

const PlayerBoxWrapper = () => {
    return (
        <div className={s.container} id={'rnd-player-box-wrapper'}>
        </div>
    );
};

export default PlayerBoxWrapper;
